# 콘솔 등록용 지면 자산

앱 화면 안에서 쓰는 웹툰 이미지는 `prototype/assets/`에 있다.
이 폴더는 토스 미니앱 목록과 챌린지 테마 지면에 **앱 바깥으로**
노출되는 자산만 둔다.

챌린지 1차 심사가 8월 지표 기반이고 트래픽 대부분이 테마 지면에서
오므로, 여기 있는 이미지가 곧 전환율이다. 코드와 같은 우선순위로
다룬다.

## 현황

| 자산 | 규격 | 상태 | 파일 |
| --- | --- | --- | --- |
| 스크린샷 1 · 컷 만들기 | 1170×2532 | 완료 | `screenshot-1-compose.png` |
| 스크린샷 2 · 이어진 릴레이 | 1170×2532 | 완료 | `screenshot-2-relay.png` |
| 스크린샷 3 · 포스터와 공유 | 1170×2532 | 완료 | `screenshot-3-poster.png` |
| 아이콘 | 600×600 PNG | 미제작 | |
| 썸네일 | 1932×828 PNG | 미제작 | |

## 스크린샷 재생성 방법

프로토타입 사본에 렌더링용 스크립트를 붙여 헤드리스 크롬으로 찍는다.
제품 코드에는 스크린샷용 분기를 넣지 않는다.

1. `prototype/`을 임시 폴더로 복사한다.
2. 사본에 `html { zoom: 3; }` 스타일과, 쿼리 `?shot=1|2|3`에 따라
   화면을 구성하는 스크립트를 추가한다.
3. `python3 -m http.server`로 사본을 띄운다.
4. 헤드리스 크롬으로 `--window-size=1170,2532 --virtual-time-budget=4000`
   옵션을 주고 `--screenshot`으로 저장한다.

`--force-device-scale-factor`는 레이아웃 뷰포트가 어긋나 오른쪽이
잘리므로 쓰지 않는다. `scrollIntoView`도 zoom과 충돌하므로 스크롤
대신 표시할 컷 수를 조절해 원하는 영역을 화면에 담는다.

## 스크린샷 순서 의도

컷 만들기 → 이어진 릴레이 → 완성 포스터 순서로 배열해, 설명 없이도
`혼자 만드는 게 아니라 이어지는 것`이라는 구조가 읽히게 한다.

## 남은 작업

- 아이콘과 썸네일 제작. 썸네일에는 앱 이름을 크게 넣지 않는다.
  지면에 이미 이름이 표기되므로 중복이고, 그 자리에 웹툰 컷이
  들어가야 탭을 딴다.
- 상단 브랜드 마크의 공 도형이 작은 크기에서 금지 표시처럼 보인다.
  아이콘 제작 전에 도형을 고쳐야 한다.
