# 한 줄 여름 V5.1 체험 점검표

대상: `prototype/`

```bash
python3 -m http.server 4184 --bind 127.0.0.1 --directory prototype
```

`http://127.0.0.1:4184/`에서 연다.

## 1. 최소 흐름

- [ ] 첫 화면의 본문 CTA는 `첫 선 그리기` 하나뿐이다.
- [ ] 결과 예시·기능 카드·분기·이력·완성 설명이 없다.
- [ ] `첫 선 그리기` 뒤 테마 선택으로 바로 이동한다.
- [ ] 한 붓 저장 뒤 본문 CTA는 `링크 복사해 토스하기` 하나뿐이다.
- [ ] 같은 기기에서 이어 그리기·중간 선 선택·완성·재생·PNG 저장·
  새 탭 열기 기능이 없다.
- [ ] 점수·승패·시간 제한·보상 문구가 없다.

## 2. 테마와 한 붓

- [ ] 선셋 비치·여름 빙수집·한강 피크닉·초록 계곡·장마 창가·
  여름밤 불꽃·하얀 캔버스가 선택된다.
- [ ] 선택한 밑그림이 Canvas와 다음 화면 미리보기에 동일하게 남는다.
- [ ] 밑그림은 사용자 펜 선보다 낮은 대비로 보인다.
- [ ] 별도 이름·장면 입력 없이 테마 선택 뒤 바로 그릴 수 있다.
- [ ] 펜 색 4종을 44px 이상 터치 버튼으로 고를 수 있다.
- [ ] 별도 시작점·끝점 표시가 없다.
- [ ] Canvas 어느 위치에서든 첫 터치 위치부터 선이 그려진다.
- [ ] 손가락을 떼면 한 붓이 끝나고 두 번째 붓은 추가되지 않는다.
- [ ] 저장 전 `지우고 다시`로 현재 붓만 다시 그릴 수 있다.
- [ ] 유효한 붓 전에는 `이 한 붓 토스하기`가 비활성이다.

## 3. 링크 토스

- [ ] `링크 복사해 토스하기` 한 번으로 현재 그림 URL이 복사된다.
- [ ] 복사 성공 시 `친구에게 보내주세요` 상태가 보인다.
- [ ] 자동 복사 실패 시에만 직접 복사용 링크 입력창이 보인다.
- [ ] 공유 URL을 열면 지금까지의 그림과 `바로 이어 그리기`만 보인다.
- [ ] 받은 화면에 선 이력·분기 수·결과 비교 버튼이 없다.
- [ ] 새 참여자도 원하는 위치에 한 붓만 더할 수 있다.
- [ ] 새 붓 저장 후 URL이 새 그림 상태로 바뀌고 다시 링크 토스만
  제안한다.

## 4. 접근성·모션

- [ ] 주요 버튼·테마 라디오·펜 색 버튼이 접근 가능한 이름을 가진다.
- [ ] Canvas에는 텍스트 대체 이름과 별도 상태 메시지가 있다.
- [ ] 키보드 초점이 보인다.
- [ ] `?motion=reduce`에서 반복 펄스와 부드러운 스크롤이 억제된다.

## 5. 레이아웃

- [ ] 390×844에서 가로 스크롤이 없다.
- [ ] 320×760에서 홈 제목·CTA가 잘리지 않는다.
- [ ] 320×760에서 테마 카드가 한 화면 폭 안에 있다.
- [ ] 320×760에서 제목·Canvas·펜 색·두 그리기 버튼이 순서대로
  보이고 가로 스크롤이 없다.
- [ ] 화면 전환 뒤 내부 스크롤이 0으로 돌아간다.
- [ ] 최소 터치 영역이 44px다.

## 6. 정적 검사

```bash
node --check prototype/app.js
git diff --check
```

- [ ] 제거한 UI ID에 이벤트 등록이 남아 있지 않다.
- [ ] 콘솔 error 0건
- [ ] 콘솔 warn 0건
- [ ] 타이밍·점수·순위·재화 코드 없음

## 확인 기록

| 날짜 | 확인자 | 환경 | 결과 |
| --- | --- | --- | --- |
| 2026-07-26 | Codex | 인앱 브라우저 390×844 | 홈·그리기·토스 단일 CTA 통과 |
| 2026-07-26 | Codex | 인앱 브라우저 320×760 | 홈·그리기·토스, 가로 넘침 없음 통과 |
| 2026-07-26 | Codex | 생성된 공유 URL 새 탭 | 미리보기·바로 이어 그리기만 노출 통과 |
| 2026-07-26 | Codex | `?motion=reduce` | 강제 동작 줄이기·가로 넘침 없음 통과 |
| 2026-07-26 | Codex | 콘솔 error/warn | 0건 |
| 2026-07-26 | Codex | 정적 검사 | JS 문법·diff 공백 검사 통과 |

미확인: 실제 모바일 기기, 화면 낭독기·키보드 전용 실사용, 실제
Apps in Toss 공유·시스템 클립보드, Supabase 동기화·동시성·만료,
사용자 재미.
