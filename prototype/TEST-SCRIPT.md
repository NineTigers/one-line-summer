# 한 줄 여름 V5 체험 점검표

대상: `prototype/`

```bash
python3 -m http.server 4184 --bind 127.0.0.1 --directory prototype
```

`http://127.0.0.1:4184/`에서 연다.

## 1. 첫 진입

- [ ] 첫 화면에서 `한 번에 한 획`, `여러 갈래`, `함께 만든 흔적`이
  보인다.
- [ ] `첫 선 그리기`와 `같은 시작, 다른 결과 먼저 보기`가 구분된다.
- [ ] 샘플 비교에서 같은 첫 선 뒤 결과가 실제로 다르다.
- [ ] 점수·승패·시간 제한·보상 문구가 없다.

## 2. 첫 선 만들기

- [ ] `떠올릴 장면` 문구와 별도 장면 제안 단계가 없다.
- [ ] 선셋 비치·여름 빙수집의 실제 그림 카드가 라디오로 선택된다.
- [ ] 선택한 그림이 Canvas·미리보기·완성 PNG에 같은 배경으로 남는다.
- [ ] 두 밑그림 모두 사용자 펜 선보다 낮은 대비로 보인다.
- [ ] 별도 이름 입력 없이 테마 선택 뒤 바로 그리기 화면으로 간다.
- [ ] 펜 색 4종을 44px 이상 터치 버튼으로 고를 수 있다.
- [ ] 빛나는 점 가까이에서 시작해야 선이 그려진다.
- [ ] 손가락을 떼면 한 획이 끝난다.
- [ ] 제출 전 `지우고 다시`로 자기 획만 다시 그릴 수 있다.
- [ ] 유효 획 전에는 `이 선 완성`이 비활성이다.

## 3. 받은 선 잇기

- [ ] 공유 URL을 새 탭에서 열면 지금까지 선을 먼저 보여준다.
- [ ] 이름 입력 없이 `바로 이어 그리기`로 다음 끝점으로 이동한다.
- [ ] 앞 선은 지워지지 않는다.
- [ ] 현재 순서가 `N번째 선`으로 보인다.
- [ ] 새 획 저장 후 URL이 새 Node 상태로 바뀐다.

## 4. 같은 중간 선 분기

1. 첫 획 저장 직후 URL을 복사한다.
2. 같은 URL을 새 탭 A와 B에서 각각 연다.
3. A와 B가 서로 다른 두 번째 획을 저장한다.
4. 원본 탭의 `중간 선을 골라 새 가지 만들기`로 이동한다.
5. `1번째` 상태를 선택한다.

확인:

- [ ] 원본 화면이 `여기서 갈라진 2개 결과 비교하기`를 표시한다.
- [ ] 비교 화면 두 카드가 동일한 첫 획을 가진다.
- [ ] 두 번째 획은 서로 다르다.
- [ ] 어느 결과에도 정답·우승·등급을 표시하지 않는다.
- [ ] 다른 가지 저장이 기존 가지를 덮어쓰지 않는다.

## 5. 네 획 완성

- [ ] `상대 화면으로 이어서 체험하기`로 A/B가 교대된다.
- [ ] 두 사람이 각각 두 번 참여하면 `이 그림 완성하기`가 활성이다.
- [ ] 완성 화면에 참여자 이름과 장면 문장이 보인다.
- [ ] 선이 저장된 순서로 다시 그려진다.
- [ ] PNG 저장 버튼이 있다.
- [ ] 완성 뒤에도 중간 선으로 돌아가 새 결과를 만들 수 있다.

## 6. 접근성·모션

- [ ] 주요 버튼·테마 라디오·펜 색 버튼이 접근 가능한 이름을 가진다.
- [ ] Canvas에는 텍스트 대체 이름과 별도 상태 메시지가 있다.
- [ ] 키보드 초점이 보인다.
- [ ] 색만이 아니라 이름·순서로 역할을 구분한다.
- [ ] `?motion=reduce`에서 반복 펄스가 억제된다.
- [ ] `?motion=reduce` 완성 화면은 재생을 기다리지 않고 즉시 `완성`이다.

## 7. 레이아웃

- [ ] 390×844에서 가로 스크롤이 없다.
- [ ] 320×760에서 홈 제목·샘플·CTA가 잘리지 않는다.
- [ ] 320×760에서 두 테마 카드가 한 화면 폭 안에 있다.
- [ ] 320×760에서 제목·그리기 Canvas·펜 색·두 버튼이 순서대로
  보이고 가로 스크롤이 없다.
- [ ] 화면 전환 뒤 내부 스크롤이 0으로 돌아가 제목이 상단바에
  가리지 않는다.
- [ ] 최소 터치 영역이 44px다.

## 8. 정적 검사

```bash
node --check prototype/app.js
git diff --check
```

- [ ] 콘솔 error 0건
- [ ] 콘솔 warn 0건
- [ ] 타이밍·점수·순위·재화 코드 없음

## 확인 기록

| 날짜 | 확인자 | 환경 | 결과 |
| --- | --- | --- | --- |
| 2026-07-26 | Codex | 인앱 브라우저 390×844 | 홈·샘플·첫 획·공유 통과 |
| 2026-07-26 | Codex | 동일 origin 새 탭 2개 | 동일 부모 실제 두 분기·비교 통과 |
| 2026-07-26 | Codex | 인앱 브라우저 | A/B 교대·4획 완성·재생 통과 |
| 2026-07-26 | Codex | 320×760 검수 프레임 | 홈·그리기 레이아웃 통과 |
| 2026-07-26 | Codex | `?motion=reduce` | 즉시 완성·반복 모션 억제 통과 |
| 2026-07-26 | Codex | 콘솔 error/warn | 0건 |
| 2026-07-26 | Codex | 인앱 브라우저 390×844 | 테마 선택·빙수집 드로잉·색 변경·선 완성 통과 |
| 2026-07-26 | Codex | 인앱 브라우저 320×760 | 두 테마 카드·선셋 드로잉·화면 전환 스크롤 보정 통과 |

미확인: 실제 모바일 기기, 화면 낭독기·키보드 전용 실사용, 실제
Apps in Toss 공유, Supabase 동기화·동시성·만료, 사용자 재미.
