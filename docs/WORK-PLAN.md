# 한 줄 여름 작업 계획

- 문서 상태: v5 활성
- 북극성: 출품 가능 수준이 아니라 실제 1등 경쟁력이 있는 제품을
  만들되, 검증되지 않은 재미와 완료를 과장하지 않는다.

## 1. 현재 완료

- [x] 비게임 공동 창작·분기 개념 고정
- [x] V4 약속 토스를 역사 명세로 이동
- [x] 정적 V5 프로토타입 전면 교체
- [x] 한 획·끝점·다시 그리기·역할 교대
- [x] 모든 중간 상태 선택·공유
- [x] 동일 부모에서 로컬 형제 가지 생성
- [x] 두 결과 비교
- [x] 네 획 완성·순서 재생·PNG 저장
- [x] 390px·320px·reduced motion·콘솔 검수

## 2. 다음 P0 · 백엔드 절단면

1. Supabase 프로젝트 소유자·지역·무료/유료 운영 결정을 기록한다.
2. `canvases`, `nodes`, `share_tokens` migration을 만든다.
3. RLS와 Edge Function을 최소 권한으로 구성한다.
4. 생성·조회·자식 추가·분기 조회 API를 구현한다.
5. 같은 부모에 동시 자식 10개를 보내 덮어쓰기 0건을 확인한다.
6. 로컬 URL 전체 문서를 공개 토큰 기반 라우팅으로 교체한다.

완료 조건: 두 브라우저가 동일 부모에서 각자 획을 저장하고 원본
흐름이 둘 다 조회한다.

## 3. Apps in Toss 절단면

1. 콘솔에서 `appName`과 비게임 카테고리를 확정한다.
2. React + TypeScript + Granite 앱 뼈대를 만든다.
3. `getAnonymousKey()` 성공·실패 경로를 연결한다.
4. `getTossShareLink()`와 `share()`를 연결한다.
5. 딥링크 `/line/:token` 직접 진입을 확인한다.
6. TDS 내비게이션·뒤로가기·안전 영역을 적용한다.
7. QR 두 실기기에서 첫 획 → 수신 획 → 분기 비교를 완료한다.

완료 조건: 샌드박스 mock이 아니라 실제 두 기기 증거가 있다.

## 4. 사용자 검증

`docs/PLAYTEST-PLAN.md`에 따라 5개 그룹을 테스트한다.

- 무설명 첫 획
- 다른 기기 수신
- 같은 중간 선을 둘 이상에게 전송
- 분기 결과 비교
- 네 획 완성
- 부담·스팸·그림 실력 불안 발화

결과는 `PASS / PASS_WITH_RISK / REWORK / STOP`으로 판정한다.

## 5. 폴리싱

사용자 가치 게이트 통과 후에만 진행한다.

- 첫 화면 카피 A/B
- 분기 비교 전환과 흐름 지도
- Canvas 성능·점 단순화
- 색 대비·스크린리더·키보드
- 오류·오프라인·만료·삭제
- 동적 공유 카드 검토

## 6. 출품

- V5 실제 앱 스크린샷 3장
- 아이콘 600×600
- 썸네일 1932×828
- 앱 설명·핵심 기능·비게임 경계
- 개인정보 처리방침·고객문의
- `.ait` 번들·콘솔 검수
- 실기기 영상과 플레이테스트 근거

## 7. 병행 작업 경계

- 이 저장소는 `한 줄 여름`만 담당한다.
- `모여름`은 별도 프로젝트·데이터·번들로 진행한다.
- `/Users/in9/YGGame.Co./projects/toss-summer-ready`를 수정하지 않는다.
- 다른 Claude/Codex 작업이 같은 파일을 편집하는지 구현 전 확인한다.
- 자동화 기능은 사용하지 않는다.
