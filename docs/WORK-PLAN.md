# 한 줄 여름 작업 계획

- 문서 상태: v5 활성
- 북극성: 출품 가능 수준이 아니라 실제 1등 경쟁력이 있는 제품을
  만들되, 검증되지 않은 재미와 완료를 과장하지 않는다.

## 1. 현재 완료

> v5.1에서 분기 비교, 중간 상태 선택, 네 획 완성, 순서 재생, PNG
> 저장을 공개 흐름에서 뺐다. 만들었다가 제거한 기능이라 완료 목록에
> 두지 않는다. 기준은 `docs/PRODUCT-SPEC.md` §5다.

- [x] 비게임 한 붓 공동 창작 개념 고정
- [x] V4 약속 토스를 역사 명세로 이동
- [x] 자유 시작 한 붓·다시 그리기
- [x] 링크 생성과 받은 링크 이어 그리기
- [x] 390px·320px·reduced motion·콘솔 오류 0건
- [x] Vite 전환과 `.ait` 번들 생성
- [x] Apps in Toss SDK 코드 연결 (실기기 미확인)
- [x] 링크 인코딩 압축과 자동 테스트
- [x] 콘솔 지면 자산·정책 페이지·등록 문안

## 2. 다음 P0 · 백엔드 절단면

1. Supabase 프로젝트 소유자·지역·무료/유료 운영 결정을 기록한다.
2. `canvases`, `nodes`, `share_tokens` migration을 만든다.
3. RLS와 Edge Function을 최소 권한으로 구성한다.
4. 생성·조회·자식 추가·분기 조회 API를 구현한다.
5. 같은 부모에 동시 자식 10개를 보내 덮어쓰기 0건을 확인한다.
6. 로컬 URL 전체 문서를 공개 토큰 기반 라우팅으로 교체한다.

완료 조건: 두 브라우저가 동일 부모에서 각자 획을 저장하고 원본
흐름이 둘 다 조회한다.

## 3. Apps in Toss 절단면

1. 콘솔에서 `appName`과 비게임 카테고리를 확정한다.
2. ~~Vite + Granite 앱 뼈대~~ 완료.
3. ~~`getAnonymousKey()` 성공·실패 경로~~ 코드 연결 완료.
4. ~~`getTossShareLink()`와 `share()`~~ 코드 연결 완료.
5. 딥링크 `?d=` 직접 진입을 **실기기에서** 확인한다. 프래그먼트가
   `deep_link_value`를 통과한다는 보장이 없어 쿼리로 실었는데, 이건
   확인된 사실이 아니라 판단이다. 틀리면 받는 쪽이 아예 안 열린다.
6. 공식 내비게이션 바·뒤로가기·안전 영역을 실기기에서 확인한다.
7. 두 실기기에서 첫 붓 → 토스 → 이어 그리기 → 재토스를 완료한다.

완료 조건: 샌드박스 mock이 아니라 실제 두 기기 증거가 있다.

## 4. 사용자 검증

`docs/PLAYTEST-PLAN.md`에 따라 5개 그룹을 테스트한다.

- 무설명 첫 붓
- 다른 기기 수신
- 받은 뒤 이어 그리기
- **이어 그린 뒤 실제 재토스**
- 부담·스팸·그림 실력 불안 발화

결과는 `PASS / PASS_WITH_RISK / REWORK / STOP`으로 판정한다.

## 5. 폴리싱

사용자 가치 게이트 통과 후에만 진행한다.

- 첫 화면 카피 A/B
- 재방문 이유 (8월 지표의 공백. 제품 결정이 먼저다)
- 색 대비·스크린리더·키보드
- 오류·오프라인·링크 깨짐 문구
- 동적 공유 카드 검토

## 6. 출품

- [x] 실제 앱 스크린샷 3장 (636×1048)
- [x] 앱 로고 600×600
- [x] 썸네일 1932×828
- [x] 앱 설명·비게임 경계 문안 (`docs/LAUNCH-TEXT.md`)
- [x] 개인정보 처리방침·고객문의 (`site/`)
- [x] `.ait` 번들
- [ ] 정책 페이지 호스팅 주소
- [ ] `appName` 콘솔 확정
- [ ] 콘솔 등록·검수
- [ ] 실기기 영상과 플레이테스트 근거

## 7. 병행 작업 경계

- 이 저장소는 `한 줄 여름`만 담당한다.
- `모여름`은 별도 프로젝트·데이터·번들로 진행한다.
- `/Users/in9/YGGame.Co./projects/toss-summer-ready`를 수정하지 않는다.
- 다른 Claude/Codex 작업이 같은 파일을 편집하는지 구현 전 확인한다.
- 자동화 기능은 사용하지 않는다.
